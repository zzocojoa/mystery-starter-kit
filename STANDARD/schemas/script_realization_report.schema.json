{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.local/schemas/script-realization-report-1.0.0.json",
  "title": "Script Realization Report v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_family",
    "schema_version",
    "project_id",
    "applicable",
    "minimum_realization_score",
    "realization_score",
    "stage_results",
    "result",
    "issues"
  ],
  "properties": {
    "$schema": {"type": "string"},
    "schema_family": {"const": "script-realization-report"},
    "schema_version": {"const": "1.0.0"},
    "project_id": {"type": "string", "pattern": "^PRJ-[0-9]{3,}$"},
    "applicable": {"type": "boolean"},
    "minimum_realization_score": {"type": "number", "minimum": 0, "maximum": 100},
    "realization_score": {"type": "number", "minimum": 0, "maximum": 100},
    "input_hashes": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "psychological_arc",
        "scene_cards",
        "presentation_plan",
        "final_script"
      ],
      "properties": {
        "psychological_arc": {"$ref": "#/$defs/hash"},
        "scene_cards": {"$ref": "#/$defs/hash"},
        "presentation_plan": {"$ref": "#/$defs/hash"},
        "final_script": {"$ref": "#/$defs/hash"}
      }
    },
    "stage_results": {
      "type": "array",
      "items": {"$ref": "#/$defs/stageResult"}
    },
    "result": {"enum": ["PASS", "FAIL"]},
    "issues": {"type": "array", "items": {"type": "object"}}
  },
  "allOf": [
    {
      "if": {"properties": {"applicable": {"const": true}}},
      "then": {
        "required": ["input_hashes"],
        "properties": {"stage_results": {"minItems": 9}}
      }
    }
  ],
  "$defs": {
    "hash": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "stageResult": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "stage_id",
        "stage_type",
        "status",
        "satisfaction_mode",
        "scene_id",
        "segment_id",
        "selector_type",
        "excerpt_hash",
        "state_delta_observed"
      ],
      "properties": {
        "stage_id": {"type": "string", "pattern": "^PSTAGE-[0-9]{3,}$"},
        "stage_type": {"type": "string", "pattern": "^[A-Z][A-Z0-9_]*$"},
        "status": {"enum": ["PASS", "FAIL"]},
        "satisfaction_mode": {"enum": ["DRAMA", "UNREALIZED"]},
        "scene_id": {"type": ["string", "null"], "pattern": "^SCN-[0-9]{2,}$"},
        "segment_id": {"type": ["string", "null"], "pattern": "^SEG-[0-9]{3,}$"},
        "selector_type": {"const": "SEGMENT_ID"},
        "excerpt_hash": {
          "anyOf": [{"$ref": "#/$defs/hash"}, {"type": "null"}]
        },
        "state_delta_observed": {"type": "boolean"}
      }
    }
  }
}
